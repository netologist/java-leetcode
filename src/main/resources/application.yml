server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: java-leetcode
  threads:
    virtual:
      enabled: true # Java 21 Virtual Threads (Project Loom) enabled for web and task execution

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
